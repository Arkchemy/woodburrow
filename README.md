## Architecture

* **Zero runtime dependencies:** Pure standard HTML5/CSS3 with inline JavaScript for filtering.

* **Shared Stylesheet (`styles.css`):** All global design tokens, elemental color definitions, component styles, and layouts are centralized in `styles.css`.

* **Vercel native:** Clean URL routing handled via `vercel.json`.

## Deployment Setup